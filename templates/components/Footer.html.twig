<footer{{ attributes }}>
    <!-- component html -->
    {% if app.current_route  != "app_contact" and app.current_route  != "app_register" %}
        
        <div class="adhesionHero">
            <div class="adhesionHero__para">
                <p>Envie de nous rejoindre ?</p>
                <p>Découvre l’association 100% famille</p>
            </div>
            <a href="{{path('app_register')}}" class="btn--footer">Je m'inscris</a>

        </div>
    {% endif %}

    <div class="footer__container">
        <div class="footer__logo">
            <img class='logo' src="{{asset("/images/logo/logomark.webp")}}" alt="logo de l'association 100% Famille">
            <img class='logo' src="{{asset("/images/logo/feader.jpg")}}" alt="logo du Feader">
            {# <span>Association 100% Famille</span> #}
        </div>

        <div class="footer__nav">
            
            <a href="{{path('app_about')}}">À propos</a>
            <a href="{{path('app_contact')}}">Nous contacter</a>
        </div>

        <div class="footer__nav">
            <a href="*">Mentions Légals</a>
            <a href="*">Politique de confidentialité</a>
        </div>
    </div>

</footer>
